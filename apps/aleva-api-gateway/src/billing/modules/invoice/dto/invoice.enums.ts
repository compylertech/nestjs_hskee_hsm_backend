export enum InvoiceType {
    SALE = 'sale',
    LEASE = 'lease',
    OTHER = 'other',
  }
  
  export enum InvoiceStatus {
    PENDING = 'pending',
    PAID = 'paid',
    EXPIRED = 'expired',
    CANCELLED = 'cancelled'
  }