export enum ContractStatusEnum {
    ACTIVE = "active",
    INACTIVE = "inactive",
    EXPIRED = "expired",
    PENDING = "pending",
    TERMINATED = "terminated"
}


export enum ContractTypeEnum {
    RENT = "rent",
    LEASE = "lease",
    PURCHASE = "purchase",
    SALE = "sale"
}
